# Configuration de la base de données PostgreSQL
# Option 1: URL complète (recommandé)
DATABASE_URL=postgresql://user:password@host:port/db_name

# Option 2: Variables séparées (utilisées si DATABASE_URL n'est pas défini)
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=

# Clé API pour protéger les endpoints d'import
# Générez une clé sécurisée avec: python -c "import secrets; print(secrets.token_urlsafe(128))"
API_KEY=your-secure-api-key-here

# Spotify API Credentials
# Obtenez vos credentials sur: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=